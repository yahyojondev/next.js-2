import React, { memo } from "react";
import Empty from "../components/empty/Empty";

const Login = () => {
  return (
    <div className="login">
      <Empty />
    </div>
  );
};

export default memo(Login);
